/**
 * Local Role enum — mirrors the Prisma schema enum.
 * Once `prisma generate` runs successfully, you can swap this import
 * for `import { Role } from '@prisma/client'` if preferred.
 */
export enum Role {
    ADMIN = 'ADMIN',
    AGENCY = 'AGENCY',
}
